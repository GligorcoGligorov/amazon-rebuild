/**
 * A seeded account so a reviewer can reach checkout without signing up. The
 * credentials are shown on the sign-in page on purpose — this is demo data with
 * no real orders and no real payment behind it.
 */
export const DEMO_EMAIL = "demo@8xstore.dev";
export const DEMO_PASSWORD = "demo1234";
export const DEMO_NAME = "Demo Shopper";
